services:
  web:
    build:
      context: .
      dockerfile: dockerfile
    container_name: python-webserver
    environment:
      - PORT=${PORT:-1000}
    ports:
      - "${PORT:-1000}:${PORT:-1000}"
    volumes:
      - "${HOST_PUBLISHED_FOLDER:-/Users/rodolphe/Public/}:/app"
